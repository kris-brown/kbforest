\title{Cartesian product (of sets)}
\date{2024-03-15}
\import{mathml}

\def\cartprod{\mml/times-{math-0004}}

\p{The \em{Cartesian product} of sets #{A} and #{B}, denoted #{A \times B}, is the set of pairs of elements where the first comes from #{A} and the second comes from #{B}.}

##{A \times B := \{(a,b)\ |\ a\in A, b \in B\}}